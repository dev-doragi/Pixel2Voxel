# Repository Rules

- 작업 전에 관련 `docs/` 문서를 읽는다.
- Core에 UI 또는 렌더러 의존성을 추가하지 않는다.
- 좌표계와 저장 형식을 임의로 확정하지 않는다.
- 공개 API를 변경할 때 대응 테스트를 추가한다.
- 작업 완료 전에 `dotnet restore`, `dotnet build`, `dotnet test`를 실행한다.
- 요청 범위 밖의 기능을 임의로 구현하지 않는다.
- 경고를 무시하거나 테스트를 삭제해 빌드를 통과시키지 않는다.

