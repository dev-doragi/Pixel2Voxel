namespace PixelVoxel.Core;

/// <summary>
/// Identifies one model-space axis without assigning storage layout semantics.
/// </summary>
public enum Axis
{
    /// <summary>The model's rightward axis.</summary>
    X,

    /// <summary>The model's upward axis.</summary>
    Y,

    /// <summary>The model's forward axis.</summary>
    Z,
}
